name: Deploy

on:
  push:
    branches:
      - staging
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.10

      - name: Install Backend Dependencies
        run: echo "Simulating backend setup"

      - name: Build Frontend
        run: echo "Simulating frontend build"

      - name: Deployment Step
        run: echo "Simulated deployment to EC2"
